# Copy to `.env` before running `docker compose up`.
# OpenAI is the default chat provider.
OPENAI_API_KEY=sk-your-key-here
# Optional: Custom OpenAI-compatible endpoint (Ollama, vLLM, LiteLLM, DeepSeek, etc.)
# OPENAI_BASE_URL=http://host.docker.internal:11434/v1
# Chat API backend: "responses" (OpenAI Responses API) or "chat" (Chat Completions).
# Defaults to "chat" when OPENAI_BASE_URL is set, "responses" otherwise.
# ERP_CHAT_API=chat

# Model configuration
# Orchestrator model for main chat and tool calling
LAMBDA_ERP_CHAT_MODEL=gpt-5.6-terra
# Report specialist model
LAMBDA_ERP_REPORT_MODEL=gpt-5.6-terra

# Optional: enable the hosted public-demo experience. When set to 1, the
# container creates a shared `public_manager` user and the chat-replay
# records so visitors can click "Enter Live Demo" without registering.
# Leave unset (or =0) for the default self-hosted flow where the first
# user to register on the login page becomes admin.
# LAMBDA_ERP_ENABLE_PUBLIC_DEMO=1

# Optional: demo spend guardrails. Only apply to the shared `public_manager`
# role — admin/manager logins are unaffected. Sliding 1-hour windows cap
# USD spend on OpenAI calls. Defaults target ~$50/day total.
# LAMBDA_ERP_DEMO_GLOBAL_HOURLY_USD=2.0833   # $50/24h
# LAMBDA_ERP_DEMO_PER_IP_HOURLY_USD=0.5208   # ~25% of default global cap
# LAMBDA_ERP_DEMO_CALL_RESERVE_USD=0.35      # pre-call reservation for burst protection
# LAMBDA_ERP_DEMO_MAX_COMPLETION_TOKENS=1024 # per-call cap on demo turns
# LAMBDA_ERP_DEMO_MAX_MESSAGE_CHARS=300      # reject demo messages longer than this
# LAMBDA_ERP_DEMO_MAX_ATTACHMENT_BYTES=102400 # reject demo attachments over 100 KiB

# Optional: pin a specific JWT signing secret. You normally don't need
# this — on first boot the backend auto-generates a secret and saves it
# to /data/.jwt_secret (next to the SQLite DB) so login cookies survive
# container restarts. Only set this if you're running multiple instances
# behind a load balancer and they all need to agree on the secret.
# JWT_SECRET_KEY=your-long-random-string
